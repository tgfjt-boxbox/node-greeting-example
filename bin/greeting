#!/usr/bin/env node

var chalk = require('chalk');

var greeting = require('..');
var hello = require('../lib/hello');

// print hello
console.log(chalk.green(hello.join('\n')));
// print waveline
console.log(chalk.blue(greeting.waveline(30)), '\n');
// print message
console.log(chalk.magenta.bold(greeting.hello()));
